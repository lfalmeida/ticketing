apiVersion: v1
kind: Service
metadata:
  name: orders-mongodb
  labels:
    name: orders-mongodb-service
spec:
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    role: orders-mongodb-service

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongodb-orders-statefulset
spec:
  serviceName: orders-mongodb
  replicas: 1
  selector:
    matchLabels:
      role: orders-mongodb-service
  template:
    metadata:
      labels:
        role: orders-mongodb-service
    spec:
      terminationGracePeriodSeconds: 10
      containers:
        - name: mongodb-orders-container
          image: mongo
          resources:
            requests:
              cpu: 0.2
              memory: 200Mi
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: orders-mongodb-persistent-storage-claim
              mountPath: /data/db
  volumeClaimTemplates:
  - metadata:
      name: orders-mongodb-persistent-storage-claim
      annotations:
        volume.beta.kubernetes.io/storage-class: "standard"
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi    